class UserAuthentication {
  final String? email;
  final String? password;
  final String? referral;
  final String? currentPassword;

  UserAuthentication(
      {this.email, this.password, this.referral, this.currentPassword});
}
